import { describe, it, expect } from "vitest";

// Smoke test verifying the Vitest configuration works.
describe("project setup", () => {
  it("runs vitest", () => {
    expect(1 + 1).toBe(2);
  });
});
